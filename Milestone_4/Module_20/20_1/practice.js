const country ="Bangladesh";
const capital = 'Dhaka';
const district = `lalmonirhat`;//preferable
console.log(district.length);
district[0]= 'rangpur'; //its immutable

const number =[1, 2, 3, 4, 5];
console.log(number.length);
number[0]= 8; // its mutable
console.log(number);