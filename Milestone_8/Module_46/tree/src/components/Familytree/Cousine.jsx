import React from 'react';
import Special from './Special';

const Cousine = ({name}) => {
    return (
        <div>
            <h3>{name}</h3>        
        </div>
    );
};

export default Cousine;